apiVersion: apps/v1
kind: Deployment
metadata:
  name: obs-web
  namespace: obs-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: obs-web
  template:
    metadata:
      labels:
        app: obs-web
    spec:
      containers:
      - name: nginx
        # Marker updated to use ondrejnr1
        image: ondrejnr1/obs-web:1.0.0 # {"$imagepolicy": "flux-system:obs-web-policy"}
        ports:
        - containerPort: 80
